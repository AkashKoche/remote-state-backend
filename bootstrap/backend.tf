# Bootstrap contains NO backend block. State remains local deliberately.
# Once `terraform apply` succeeds, you will migrate the state to the
# newly created S3 bucket. Reference Step 6 for execution commands.
